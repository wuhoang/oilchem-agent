# 文件访问范围说明

## 可访问根目录
- H:\trae-project\oilchem-agent

## 支持的文件类型
- 文本文件：CSV、JSON、Markdown、Python、TXT、YAML、LOG 等
- 二进制文件：仅返回元信息（大小、修改时间等）

## 支持的操作
- 读取文件内容（支持指定行范围）
- 写入文件（覆盖或新建）
- 追加内容到文件末尾
- 列出目录内容（支持递归和模式过滤）
- 删除文件

## 目录结构概览
（此处根据实际 list_files 结果填充）